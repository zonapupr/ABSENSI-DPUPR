export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({ok:false,message:'POST only'});const gas=process.env.GAS_WEB_APP_URL,key=process.env.GAS_GATEWAY_KEY;if(!gas||!key)return res.status(500).json({ok:false,message:'Environment Vercel belum lengkap'});try{const r=await fetch(gas,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...req.body,gatewayKey:key}),redirect:'follow'}),t=await r.text();try{return res.status(200).json(JSON.parse(t))}catch(_){return res.status(502).json({ok:false,message:'Respons GAS tidak valid',details:t.slice(0,400)})}}catch(e){return res.status(502).json({ok:false,message:e.message||'Gateway error'})}}
